let selectedColors = ['red', 'blue']; //initialized to an empty array
//[] are empty literals
//objects and size of array are dynamic
selectedColors[2] = 1;


//log on the console
console.log(selectedColors);

//access an element in array
console.log(selectedColors[0]);

//can access properties of array objects
//length returns lengths of array
console.log(selectedColors.length);
